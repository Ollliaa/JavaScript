// Даны два числа x и y, а также переменная sign. Переменная sign  содержит знак математической операции ('+', '-', '*', '/')
// которую надо выполнить над числами x и y.
// Напишите команду для вычисления суммы, разности, произведения или частного чисел x и y (в зависимости от значения переменной sign)
// Если знак операции не является одним из перечисленных знаков, результат должен быть равен 'unknown value'.
// Задайте переменные x и y  и присвойте им значения произвольных чисел.
// Задайте переменную sign и присвойте ей значение строки, содержащей знак математической операции ('+', '-', '*', '/').
// Задайте переменную result.
// Используя оператор switch, получите в переменной result значение:
// x + y, если sign = "+";
// x - y, если sign = "-";
// x * y, если sign = "*";
// x / y, если sign = "/";
// 'unknown value', в противном случае.


let x = 5;
let y = 3652;
let sign = "*";
let result;

switch (sign) {
    case "+" : result = x + y; break;
    case "-" : result = x - y; break;
    case "*" : result = x * y; break;
    case "/" : result = x / y; break;
    default: result = "unknown value";
}
console.log(result);